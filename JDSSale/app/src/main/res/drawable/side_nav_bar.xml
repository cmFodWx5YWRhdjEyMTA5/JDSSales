<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <gradient
        android:angle="135"
        android:centerColor="@color/driver"
        android:endColor="@color/driver"
        android:startColor="@color/driver"
        android:type="linear" />
</shape>